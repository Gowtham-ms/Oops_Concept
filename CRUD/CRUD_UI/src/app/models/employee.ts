export interface Employee{
    // this variable will catch the values from api to angular 
    employeeId : any, // this is coming from api
    employeeName : string,
    mobileNumber : number,
    emailID : string
}