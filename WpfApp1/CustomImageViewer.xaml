<UserControl x:Class="WpfApp1.CustomImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="521"
             MouseLeftButtonDown="CustomImageViewer_MouseLeftButtonDown">
    <Grid>
        <Image x:Name="image" Stretch="Uniform" Width="Auto" MouseLeftButtonDown="image_MouseLeftButtonDown" MouseMove="image_MouseMove" MouseLeftButtonUp="image_MouseLeftButtonUp">
            <Image.RenderTransform>
                <TranslateTransform/>
            </Image.RenderTransform>
        </Image>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Button x:Name="ZoomInButton" Content="Zoom In" Margin="5,0,5,0" Visibility="Collapsed" Click="ZoomInButton_Click"/>
            <Button x:Name="ZoomOutButton" Content="Zoom Out" Margin="5,0,5,0" Visibility="Collapsed" Click="ZoomOutButton_Click"/>
        </StackPanel>
        <Button x:Name="CloseButton" Content="Close" HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Collapsed" Click="CloseButton_Click"/>
    </Grid>
</UserControl>
